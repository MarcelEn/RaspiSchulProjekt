import {
    actionNames
} from '../../actions';

export default (state = {
    editingCalendar: null,
    showDeleteWarning: false
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
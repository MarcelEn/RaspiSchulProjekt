import {
    actionNames
} from '../../actions';

import {
} from '../../globalFunctions';

export default (state = {
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
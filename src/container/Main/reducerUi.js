import {
    actionNames
} from '../../actions';

import {
    getCookie
} from '../../globalFunctions';

export default (state = {
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}